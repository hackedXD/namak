{
  "name": "@salt/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Cloudflare Workers + Hono API (design §5). Wires the pure engines to D1.",
  "main": "src/index.ts",
  "scripts": {
    "test": "NODE_OPTIONS='--experimental-sqlite --no-warnings' vitest run",
    "typecheck": "tsc --noEmit",
    "lint": "echo noop && exit 0"
  },
  "dependencies": {
    "@salt/domain": "workspace:*",
    "@salt/db": "workspace:*",
    "@salt/pricing": "workspace:*",
    "@salt/equivalence": "workspace:*",
    "hono": "^4.6.14",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vitest": "^2.1.8",
    "@types/node": "^22.10.0"
  }
}

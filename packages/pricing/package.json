{
  "name": "@salt/pricing",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "§4.4 price ladder engine. Pure given inputs (no fetching). Ranks, ceiling, overcharge.",
  "main": "src/index.ts",
  "exports": { ".": "./src/index.ts" },
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "lint": "echo noop && exit 0"
  },
  "dependencies": { "@salt/domain": "workspace:*", "@salt/equivalence": "workspace:*" },
  "devDependencies": { "typescript": "^5.7.2", "vitest": "^2.1.8" }
}

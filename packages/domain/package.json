{
  "name": "@salt/domain",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Pure shared domain types (Molecule, Formulation, Result). Zero I/O.",
  "main": "src/index.ts",
  "exports": { ".": "./src/index.ts" },
  "scripts": {
    "test": "echo '@salt/domain: types only, no tests' && exit 0",
    "typecheck": "tsc --noEmit",
    "lint": "echo noop && exit 0"
  },
  "devDependencies": { "typescript": "^5.7.2" }
}

{
  "name": "@salt/schema",
  "version": "0.0.0",
  "private": true,
  "description": "Salt canonical SQL schema (D1/SQLite) + migrations. Source of record for the DB contract.",
  "type": "module",
  "scripts": {
    "test": "node --experimental-sqlite --no-warnings scripts/validate.mjs",
    "typecheck": "echo \"@salt/schema: no TS yet (SQL-only package)\" && exit 0",
    "lint": "echo \"@salt/schema: lint noop\" && exit 0"
  }
}

{
  "name": "@salt/db",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Repository interfaces + D1/SQLite implementations. The only package that writes SQL.",
  "main": "src/index.ts",
  "exports": { ".": "./src/index.ts" },
  "scripts": {
    "test": "NODE_OPTIONS='--experimental-sqlite --no-warnings' vitest run",
    "typecheck": "tsc --noEmit",
    "lint": "echo noop && exit 0"
  },
  "dependencies": { "@salt/domain": "workspace:*", "@salt/geo": "workspace:*" },
  "devDependencies": { "typescript": "^5.7.2", "vitest": "^2.1.8", "@types/node": "^22.10.0" }
}
